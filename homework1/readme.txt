1. Проблем не возникло
2. Ок
3.
var_dump(3 / 1); int(3)
var_dump(1 / 3); float(0.33333333333333)
если в результате деления целое число, то int, если нет, то float

var_dump('20cats' + 40); int(60)
var_dump('20.5cats' + 40); float(60.5)
т.к. оператор +, то операнды приводятся к числу, берется сначала строки все, что похоже на число, а остальное отбрасывается

var_dump(18 % 4); int(2)
целочисленный остаток от деления 18 на 4.

4.
то что справа от = , т.е. то что присваивается

5.
var_dump(1 == 1.0); bool(true)
происходит преобразование к одному типу

var_dump(1 === 1.0); bool(false)
тут еще сравнивается и тип, поэтому false

var_dump('02' == 2); bool(true)
var_dump('02' === 2); bool(false)
var_dump('02' == '2'); bool(true)

если сравнивается строка с числом или две строки, содержащие число, то строки преобразовываются в числа и сравниваются
в случае с === преобразование типов не происходит

6.
дело в приоритете операторов, сначала происходит присваивание, поэтому true